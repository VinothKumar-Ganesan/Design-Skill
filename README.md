# Designing Skill CLI

Beginner Node.js CLI that scaffolds simple design templates (landing page, logo, mobile UI) and produces checklists.

## Setup

Install dependencies:

```bash
npm install
```

Run the CLI:

```bash
npm start
# or when installed globally: design-skill
```

Generated projects are placed in `output/<name>`.
